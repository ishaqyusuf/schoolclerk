import { PdfConfig } from "./create-pdf";

export const defaultPdfConfig: PdfConfig = {
    format: "Letter",
    margin: {
        left: "0.39in",
        top: "0.39in",
        right: "0.39in",
        bottom: "0.39in",
    },
};
