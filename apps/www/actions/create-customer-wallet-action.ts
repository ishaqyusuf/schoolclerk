"use server";

import { prisma } from "@/db";

export async function createCustomerWalletAction({ accountNo }) {
    // const c = await prisma.customerWallet.create({
    //     where: {
    //         accountNo,
    //     },
    //     create: {
    //         balance: 0,
    //         accountNo,
    //     },
    //     update: {
    //     }
    // });
}
