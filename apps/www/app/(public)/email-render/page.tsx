import { render } from "@react-email/components";

export default async function EmailRender({}) {
    return (
        <div className="dotted-bg flex min-h-screen flex-col items-center justify-center p-4 sm:p-6 md:p-0 ">
            <div className="">
                <p>LOREM</p>
            </div>
        </div>
    );
}
